version: 1
services:
  - type: web
    name: my-rasa-service
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && rasa train
    startCommand: rasa run --enable-api --cors "*"
    env:
      PYTHON_VERSION: "3.7.9"  # Ensure this is a string
